# 部署langgraph的尝试（7.20起）

## 一， 环境准备

### 1，python配置检查、认识虚拟环境

```
                 Python解释器
                 D:\py\python.exe
                       |
                       |
          -----------------------------
          |                           |
          ↓                           ↓

   LangGraph项目                 pyxamstore项目

   D:\aitool\LangGraph          D:\reverse\pyxamstore

          |                           |
          |                           |

       venv                         venv

          |                           |

  独立site-packages          独立site-packages

          |                           |

 langgraph                  pyxamstore
 langchain                  xxhash==3.2
 openai                     ...
```
venv的原理是基于主Python创建一个隔离环境，其中包含独立的 Python 运行入口、pip 和第三方包目录。项目通过这个环境运行。

创造（本机）：(路径)python.exe -m venv venv

升级隔离环境pip ：python -m pip install --upgrade pip

uv版本：

uv venv .venv --python 3.13(可换)

call .venv\Scripts\activate

uv pip install requests pyelftools
### 2，部署langgraph
- 启用venv: (路径)venv/Script/activate
- 安装langgraph框架：python -m pip install langgraph langchain langchain-openai
- 安装基础开发依赖 python -m pip install python-dotenv

- 创造项目
```
LangGraph
│
├── venv（隔离python包）
│
├── main.py
│
├── .env   （密钥）
│
└── requirements.txt
```

- 保存环境 pip freeze > requirements.txt

## 二，进入学习

### 1，写第一个api调用
```
from openai import OpenAI
import os
from dotenv import load_dotenv


# 读取.env
load_dotenv()


# 创建客户端
client = OpenAI(
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com"
)


response = client.chat.completions.create(
    model="deepseek-v4-pro",
    messages=[
        {
            "role": "user",
            "content": "你好，介绍一下你自己"
        }
    ]
)


print(
    response.choices[0].message.content
)
```


### 2，变为多次单条访问
```
......

while 1:

    response = client.chat.completions.create(
    model="deepseek-v4-pro",
    messages=[
        {
            "role": "user",
            "content": input("")
        }
    ]
)


    print(
    response.choices[0].message.content
)


```

### 3，引入message []变为可存储每轮上下文（但没有精简存储，上下文污染严重,只是上下文记忆）

```
........

while 1:

    user_input=input("[USER]:")

    if user_input == "exit" : break

    #添加用户消息
    messages.append(
        {
            "role":"user",
            "content" : user_input
        }
    )


    response = client.chat.completions.create(
    model="deepseek-v4-pro",
    messages=messages
)

    answer = response.choices[0].message.content

    print(
    "[DeepSeek]:",answer
)

```

### 4，引入prompts(引入system预设，稳定规范LLM逻辑，但还是可能会有较为突兀的信息回显)
```
messages = [
    {
        "role":"system",
        "content":
        """
        你现在是一名Python老师。
        
        教学规则：
        1. 我是初学者。
        2. 不要直接给答案，要解释原理。
        3. 每次解释包含：
           - 概念
           - 示例
           - 常见错误
        """
    }
]
```
#### 本质

Prompt不是一句话。

而是发送给LLM的完整messages。


#### 消息角色

system:
定义身份和规则

user:
用户输入

assistant:
历史回复


#### Agent关系

Prompt:告诉模型如何思考

State:保存信息

Tool:提供能力

Workflow:控制流程

### 5，模板化与参数化
- 模板抽象表示：
```
你是一名{role}。

你的任务：      
帮助用户学习{topic}。
```
      
### 关于语言管理特征
- maven      pom.xml
- c++       CMakeLists.txt
- python        requirements.txt / uv.lock/pyproject.toml

*uv和pip体验区别：*

*传统:*
```
创建环境

python -m venv .venv

激活

pip install -r requirements.txt
```
*uv(rust写的)*
```
uv sync
```

- 通用  .json等